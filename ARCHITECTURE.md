# سند / Sanad — المرجع التقني الكامل

> هذا الملف مكتوب ليُقرأ كاملاً من قبل نموذج لغوي قبل أن يكتب أي تعديل على الموقع.
> كل ما فيه مأخوذ من الشيفرة نفسها، لا من نيّة أو خطة. الأسماء التقنية بالإنجليزية
> كما هي في الملفات، والشرح بالعربية.

---

## 0. ملخص في عشرة أسطر

| | |
|---|---|
| **ما هو** | منصة سعودية تربط العميل بمحامٍ مرخّص: استشارة، مراجعة عقد، صياغة، مذكرة. |
| **اللغة** | HTML + CSS + JavaScript (ES5 عادي، `var`، بلا `class`) — **بلا إطار، بلا خطوة بناء، بلا npm في الإنتاج**. |
| **قاعدة البيانات** | PostgreSQL عبر **Supabase**، والحماية كلها **Row Level Security**. |
| **الوصول للبيانات** | `fetch` مباشر إلى **PostgREST** (`assets/js/core/rest.js`) — مكتبة supabase-js تُحمَّل كسولاً لثلاثة أشياء فقط: تسجيل الدخول، Realtime، رفع الملفات. |
| **المستخدمون** | زائر، عميل `client`، محامٍ `lawyer`، متدرب `intern`، موظف إدارة `staff`. |
| **الصفحات** | ١٨ صفحة HTML، كل واحدة تسجّل نفسها بـ `Pages.define("name", fn)`. |
| **اللغات** | عربي (افتراضي، RTL) وإنجليزي (LTR)، قاموسان داخل `assets/js/core/i18n.js`. |
| **الثيم** | فاتح/داكن عبر `data-theme` ومتغيرات CSS. |
| **المكالمات** | WebRTC مباشر بين المتصفحين + إشارات عبر Supabase Realtime، مع تسجيل. |
| **النشر** | GitHub Actions → فرع `gh-pages` → موقع ثابت. |

---

## 1. البنية العامة — كيف تعمل الصفحة

الموقع **متعدد الصفحات (MPA) ثابت**. لا خادم تطبيقات، لا SSR، لا router. كل ملف
`.html` صفحة قائمة بذاتها، تحمّل نفس ملفات JS بترتيب ثابت.

### ترتيب التحميل في كل صفحة

```
config.js          ← الإعدادات: أي backend، ومفاتيح Supabase العامة
core/theme.js      ← يقرأ الثيم قبل الرسم حتى لا تومض الصفحة
core/i18n.js       ← القاموسان + دوال الترجمة والأرقام والتواريخ
ui/icons.js        ← أيقونات SVG inline
core/store.js      ← مصدر البيانات المحلي (المتصفح)
data/seed.js       ← بيانات العرض (لا تظهر إطلاقاً على backend حقيقي)
data/models.js     ← كل المنطق المشتق: الأسعار، الحالات، الترتيب، الحسابات
core/session.js    ← من المستخدم الحالي وما دوره
core/rest.js       ← PostgREST عبر fetch
core/supabase.js   ← تسجيل الدخول، الترطيب (hydrate)، تحميل المكتبة كسولاً
core/store.remote.js ← يستبدل Store بنسخة تتكلم مع Supabase
core/signal.js     ← قناة الإشارات للمكالمات
core/rtc.js        ← WebRTC + مسجّل المكالمة
ui/components.js   ← كل المكوّنات المرئية المشتركة
ui/layout.js       ← الهيدر والفوتر والتنقّل
core/app.js        ← نواة التشغيل: rerender، toast، تبديل اللغة والثيم
pages/<page>.js    ← منطق هذه الصفحة وحدها
```

### قاعدتان تحكمان كل شيء فوق `store.js`

1. **كل قراءة متزامنة.** `Store.requests()` يُعيد مصفوفة فوراً، لا Promise.
   حتى مع Supabase: `store.remote.js` يحتفظ بكاش في الذاكرة، يملؤه مرة عند الإقلاع،
   والكتابة تدخل الكاش فوراً وتذهب لقاعدة البيانات خلفها.
2. **إعادة الرسم مركزية.** كل صفحة تسجّل `App.onRender(fn)`، وتُستدعى تلقائياً عند
   `langchange` و`sessionchange` و`storechange`. لا يوجد DOM diffing — تُبنى سلسلة
   HTML وتُسنَد إلى `innerHTML`، ثم `I18N.apply(host)`.

### مصدرا البيانات

`SANAD_CONFIG.backend` في `assets/js/config.js`:

* `"browser"` — كل شيء في `localStorage` + `sessionStorage`، مع أشخاص وهميين من
  `seed.js`. هذا وضع العرض والاختبارات.
* `"supabase"` — المشروع الحقيقي. **`seeded()` في models.js تُرجع مصفوفة فارغة هنا**،
  أي لا يوجد محامٍ وهمي واحد في الإنتاج. الموقع يبدأ فارغاً، وهذا مقصود.

الوضع الحالي في المستودع: `backend: "supabase"`.

---

## 2. شجرة الملفات

```
*.html                    ١٨ صفحة (انظر §6)
assets/css/style.css      ورقة أنماط واحدة، ~٢٠٠٠ سطر، متغيرات CSS للثيمين
assets/js/config.js       الإعدادات والمفاتيح العامة
assets/js/core/           app, i18n, rest, rtc, session, signal, store,
                          store.remote, supabase, theme
assets/js/data/           models.js (المنطق) — seed.js (بيانات العرض)
assets/js/pages/          ملف لكل صفحة
assets/js/ui/             components.js, icons.js, layout.js
supabase/schema.sql       المخطط الأساسي
supabase/migrations/      ١٧ هجرة مرقّمة 002 → 018
supabase/rls-test*.sql    ١٤ ملف اختبار صلاحيات (١٩٤ تحقّق)
tools/*.mjs               ٣٠+ حزمة اختبار Playwright/Node
tools/run-all.sh          تشغّلها كلها
tools/migrate-test.sh     تطبّق كل هجرة على قاعدة فيها بيانات حقيقية
.github/workflows/pages.yml  النشر
```

---

## 3. قاعدة البيانات — كل جدول

قاعدة البيانات PostgreSQL على Supabase. **كل** جدول عليه `enable row level security`.
المفتاح الذي في الصفحة هو `publishable` فقط؛ ما يحمي البيانات هو السياسات، لا سرية المفتاح.

### 3.1 الأشخاص

**`profiles`** — صف لكل مستخدم، مفتاحه هو `auth.users.id`.

| عمود | نوع | ملاحظة |
|---|---|---|
| `id` | uuid PK | = `auth.users.id` |
| `full_name`, `phone`, `city`, `avatar_url`, `bio`, `title` | text | |
| `roles` | text[] | `client` / `lawyer` / `intern` / `staff` |
| `active_role` | text | أي دور يتصفّح به الآن |
| `status` | text | `pending` \| `verified` \| `rejected` |
| `licence_no`, `licence_authority`, `licence_expiry`, `licence_file_url` | | للمحامي |
| `specialties` text[], `years` int | | للمحامي |
| `university`, `level`, `cv_url`, `skills` text[] | | للمتدرب |
| `title` | text | اللقب المهني — يُقرأ على كل بطاقة |
| `email` | text | يُثبَّت بـ`keep_email()` |
| `featured_rank`, `featured_until` | | التمييز في الدليل |
| `vat_registered`, `vat_number` | | هل المحامي مسجّل ضريبياً |
| `auto_bid` | boolean | يقدّم عروضه في المزاد تلقائياً |
| `onboarded` | boolean | أكمل خطوة «من أنت» بعد دخول Google |
| `rejected_reason` | text | سبب رفض الرخصة، يبقى مع الحساب |

**قواعد صارمة على هذا الجدول:**
* `keep_status()` — trigger يمنع صاحب الحساب من تغيير `status` بنفسه. الاعتماد من الإدارة فقط.
* `guard_roles()` — يمنع أي مستخدم من **إضافة** `staff` لنفسه، ويمنع **إزالتها** أيضاً
  (هجرة 005: موظف يجيب على سؤال «من أنت» كان يكتب نفسه خارج المكتب).
* `handle_new_user()` — trigger على `auth.users` ينشئ صف `profiles` تلقائياً، حتى يعمل
  الدخول عبر Google بلا مرور بمعالج التسجيل.
* القراءة عامة (`using (true)`) لأن الدليل والمقالات والتقييمات تعرض الملف الشخصي.

### 3.2 الخدمات والتسعير

**`service_types`** — كتالوج العمل القانوني (`id` نصي: `consult`, `review`, `contract`,
`express`, `drafting`…). يحمل `title_ar/en`, `meta_ar/en`, `icon`, `channels text[]`,
`sort`, `active`. الإدارة تضيف تصنيفاً بلا إصدار جديد.

**`price_bands`** — `type_id` → `min_price` / `max_price`. **النطاق في قاعدة البيانات**،
لا في النموذج فقط: `check_price_band()` trigger يرفض أي سعر خارجه.

**`services`** — ما يعرضه محامٍ بعينه: `owner_id`, `type_id`, `title`, `price`,
`channels text[]`, `active`.

### 3.3 الطلبات — الجدول المركزي

**`requests`**

| عمود | ملاحظة |
|---|---|
| `id` uuid | |
| `ref` text | **الرقم المرجعي** `SND-26-00042` — من `stamp_ref()` + sequence |
| `client_id`, `lawyer_id`, `assigned_to` | العميل، المحامي، المتدرب |
| `type_id`, `title`, `brief`, `price` | |
| `channel` | `text` \| `voice` \| `video` — **كيف** يُنفَّذ العمل |
| `status` | انظر §5 |
| `ai_assisted`, `body` | `body` هو المُخرَج المُسلَّم |
| `intern_share` int | ٣٠–١٠٠٪ نصيب المتدرب |
| `hours` int | ساعات التدريب المحتسبة |
| `delivered_at`, `accepted_at` | ساعتا نافذة القبول والضمان — يكتبهما `stamp_request_clock()` |
| `revisions`, `revision_note` | طلب تعديل واحد كحد أقصى |
| `rated` boolean | هل قيّم العميل المحامي — يكتبه `mark_request_rated()` |

**Triggers على `requests`:**
* `stamp_ref()` — يعطي رقماً مرجعياً عند الإنشاء.
* `stamp_request_clock()` — يختم `delivered_at` عند `delivered` و`accepted_at` عند `completed`.
* `pin_request_terms()` — يثبّت الشروط بعد التسليم: لا يُغيَّر السعر ولا نصيب المتدرب لاحقاً، وطلب مُسترد يبقى مُسترداً.
* `guard_status()` — يمنع قفزات الحالة غير المشروعة.
* `guard_revisions()` — تعديل واحد فقط.
* `guard_one_open_request()` — انظر §11.
* `log_request_event()` — يكتب في `request_events`.

### 3.4 المحادثات والملفات

**`messages`** — `request_id`, `author_id`, `audience`, `body`, `created_at`.
**`attachments`** — نفس الشيء + `message_id`, `path` (فريد، في Storage), `name`, `size`,
`mime`, **`kind`** (`call` للتسجيلات), **`seconds`**.

**`audience` — ثلاثة جماهير، وهذا أهم مفهوم في المنصة:**

| القيمة | من يقرأ | ما هو |
|---|---|---|
| `parties` | العميل + المحامي + المتدرب المكلّف | محادثة القضية: ما رفعه العميل وما ردّ به المحامي |
| `internal` | المحامي + المتدرب فقط (**العميل لا يراها إطلاقاً**) | الإشراف: «راجع الاختصاص قبل ما تكتب المذكرة» |
| `staff` | الإدارة فقط (**لا العميل ولا المحامي ولا المتدرب**) | نسخة المنصة من تسجيل المكالمة |

السياسة نفسها مكتوبة مرة واحدة على الجدولين:
```sql
case audience
  when 'staff'    then public.is_staff()
  when 'internal' then public.works_on_request(request_id)
  else                 public.may_see_request(request_id)
end
```

الملفات في **Supabase Storage**، دلو خاص اسمه **`case-files`**، مجلّد لكل طلب.
لا شيء فيه عام: القراءة عبر رابط موقّع (`signUrl`, صلاحية ساعة).

### 3.5 السجل

**`request_events`** — `request_id`, `kind`, `by_id`, `detail`, `at`.
أنواع `kind`: `placed`, `lawyer_set`, `taken`, `assigned`, `unassigned`,
`status:<الحالة>`, `revision`, `disputed`, `decided`.

> ملاحظة مقصودة: **لا يوجد trigger ينسخ الرسائل إلى `request_events`**، لأن العميل
> يستطيع قراءة هذا الجدول، وذلك كان سيكشف وجود المحادثة الداخلية له. الرسائل تُدمج مع
> الأحداث في الواجهة فقط (`Models.timeline`)، وكلٌّ محكوم بسياسته.

### 3.6 الاعتراضات

**`disputes`** — `request_id` (فريد: اعتراض واحد لكل طلب)، `ref` (`OBJ-26-00007`)،
`by_id`, `reason`, `status` (`open`/`resolved`), `outcome` (`release`/`refund`/`split`),
`lawyer_pct`, `resolution_reason`, `resolved_by`, `resolved_at`.

قيود: قرار `split` بلا نسبة مرفوض، وقرار بلا سبب مكتوب مرفوض.
`guard_dispute()` يمنع تعديل اعتراض مقرَّر: **«a dispute is decided once»** —
لكن مع مسار موثوق (`auth.uid() is null`) حتى تستطيع الهجرات والسكربتات صيانة الصف.
`tell_staff_about_dispute()` يُشعر الإدارة تلقائياً.

### 3.7 المال

**`payments`** — `request_id`, `client_id`, `gateway`, `gateway_ref`, `amount` (هللات),
`service_vat`, `status` (`held`/`released`/`refunded`/`frozen`).
**`payouts`** — `payment_id`, `party` (`platform`/`lawyer`/`intern`/`client`), `amount`, `vat`, `pct`.

> **حالة الواقع:** الجدولان موجودان بسياساتهما، لكن **لا توجد بوابة دفع موصولة بعد**.
> الواجهة تحسب المبالغ في `models.js` ولا تكتب في `payments`. أي عمل على الدفع الحقيقي
> يبدأ من هنا.

**`platform_settings`** — صف واحد فقط (`id = 1`، وقيد `check (id = 1)` يمنع الثاني):
`commission_pct` (مسقوفة بـ ١٠٪)، `vat_enabled`، `vat_pct`،
رسوم البوابة `mada_pct` / `mada_fixed` / `mada_share_pct` و`card_pct` / `card_fixed`،
`ai_price` (اشتراك أداة الصياغة)، `guarantee_hours` (٢٤)،
`guarantee_unconditional` (true)، `min_years` (٥).

**`operating_costs`**, **`partners`** (نسب الشركاء), **`subscriptions`** (اشتراك المحامي في أداة الصياغة).

### 3.8 المزاد العكسي

**`quotes`** — طلب عروض: `client_id`, `type_id`, `channel`, `brief`, `city`, `specialty`,
`status` (`open`/`accepted`/`expired`/`cancelled`), `accepted_by`, `request_id`, `expires_at`.
**`offers`** — `quote_id`, `lawyer_id`, `price`, `eta`, `auto`, `note`. فريد لكل (مزاد، محامٍ).
`check_offer_band()` يفرض النطاق السعري على العرض أيضاً.
`auto_bid_on_new_quote()` يقدّم عروضاً تلقائية لمن فعّلها.

**`applications`** — تقدّم متدرب لمهمة فتحها محامٍ لكل المتدربين.

### 3.9 الصياغة والورش والإشراف (هجرة 019)

**`draft_jobs`** — طابور المسوّدات: `request_id` (فريد)، `lawyer_id`، `status`
(`queued`/`running`/`ready`/`failed`/`used`)، `body`، `model`، `context_ref`، `attempts`.
يُملأ بـtrigger حين يصل عمل لمحامٍ **مشترك** في أداة الصياغة، بالطريقين: أن يكون على الطلب
من البداية أو أن يستلمه من المزاد. لا يقرأه العميل، ولا يحذفه المحامي.
سؤال الاشتراك يُسأل مرة واحدة: **`has_ai(lawyer)`**.

**`webinars`** + **`webinar_seats`** — ورشة يستضيفها محامٍ موثَّق: `ref` (`WRK-26-00001`)،
`seats`، `price`، `starts_at`، `minutes`، `status`. المقعد يُحجز مرة لكل شخص،
والسقف محروس بقفل صف (`guard_seats`)، والسعر **مثبَّت على المقعد** لحظة الحجز.
الإعلان عام؛ **قائمة الحاضرين ليست عامة** — صاحب المقعد والمضيف والإدارة فقط.

**`mentorships`** — الإشراف المدفوع: `mentor_id`، `intern_id`، `opened_by`
(`intern` تقدّم / `mentor` دعا)، `status`، `fee`، `paid_until`.
**الطرف الذي طلب لا يَقبل** — `guard_mentorship()` يرفض ذلك.
**`mentorship_messages`** — غرفة الإشراف (الاثنان والإدارة).
**`mentorship_sessions`** — التقويم؛ الجلسة الحاضرة تُحتسب ساعاتها عبر `mentored_hours()`.

### 3.10 الفرز المجاني والخصومات (هجرة 020)

**`free_screening`** — تصنيف في `service_types` بنطاق سعري `0..0`.
`guard_screening()` يرفض أي سعر عليه، ويشترط أن يكون المتدرب الذي يستلمه تحت إشراف
**فعّال**، ثم **يضع مشرفه محامياً على الطلب** — لأن من يجيب هو من يتحمّل الإجابة.
قاعدة «طلب واحد» صارت تقارن المِثل بالمِثل: جلسة فرز لا تحجب عملاً مدفوعاً ولا العكس،
لكن جلستَي فرز مفتوحتين مرفوضتان.

**كفالة الإشراف** — `platform_settings.sponsorship_pct` (≤ ٢٠٪) و`sponsorship_min/max`
(٥٠–١٠٠ ر.س)، و`guard_mentorship_fee()` يفرض النطاق.
`payments.request_id` صار قابلاً لأن يكون فارغاً مع `mentorship_id`، وقيد يقول
**واحد منهما لا كلاهما ولا لا شيء**. الشحن عبر
**`charge_sponsorship(mentorship, gateway, ref, months)`** — دالة `definer` تكتب
`payments` + سطرَي `payouts` (حصة المنصة، وصافي المحامي)، لأن الجدولين **لا يقبلان كتابة من
المتصفح** (قرار هجرة 003 وبقي كما هو).

**`promo_codes`** — `code`, `discount_pct`, `max_discount` (هللات), `usage_limit`,
`used_count`, `client_id` (كود شخصي), `type_id`, `expires_at`, `active`.
**`promo_redemptions`** — من صرف ماذا؛ مفتاح فريد `(promo_id, client_id)` هو ما يمنع
الصرف مرتين، ولا سياسة إدراج عليه إطلاقاً.
`validate_promo_code(code, gross, type)` تُعيد `(ok, reason, discount, pct)`،
و`redeem_promo_code(code, request)` تصرفه تحت قفل.

> **الخصم يخرج من عمولة المنصة، لا من نصيب المحامي ولا المتدرب** — ولذلك هو **مسقوف
> بالعمولة نفسها**. كود يعطي أكثر مما تكسبه المنصة على الطلب يُعطي العمولة كاملة ويقول
> `capped at our commission` بدل أن يَعِد برقم لا يستطيع الدفتر الوفاء به.

وحين تكتمل جلسة فرز، `offer_conversion()` يُصدر للعميل **كوداً حقيقياً باسمه**
(١٠٪، صلاحية ٣٠ يوماً، استعمال واحد) بدل لافتة.

### 3.11 البقية

**`agreements`** — اتفاق دائم بين محامٍ ومتدرب: `kind` (`cases`/`monthly`/`yearly`) و`amount`.
عمل تحت اتفاق **لا يأخذ شيئاً من الطلب** — يُحسب بين الطرفين خارجه.
**`articles`**, **`comments`** — المدونة. مقال المتدرب `pending` حتى يوقّعه محامٍ.
**`reviews`** — تقييم من ١ إلى ٥. السياسة: **لا يقيّم إلا عميل طلبٍ سُلّم**، وواحد لكل طلب.
**`endorsements`** — شهادة تدريب يوقّعها المحامي المشرف.
**`notifications`** — `to_id`, `type`, `ref`, `read`. **لا تحمل محتوى أبداً** — تقول إن شيئاً تحرّك وتشير إليه.
**`announcements`** — إعلانات الإدارة، بجمهور مستهدف.
**`audit_log`** — كل قرار إداري: `action`, `by_id`, `target_id`, `subject`, `reason`.
`audit_is_final()` يمنع التعديل والحذف — يُكتب ولا يُعدَّل.

---

## 4. الحماية (RLS) — الدوال الأربع التي يقوم عليها كل شيء

```sql
is_party(r)              -- auth.uid() هو العميل أو المحامي أو المتدرب على هذا الصف
may_see_request(rid)     -- أحد الثلاثة، أو الإدارة
works_on_request(rid)    -- المحامي أو المتدرب فقط، أو الإدارة
is_staff()               -- 'staff' في roles
is_lawyer()              -- 'lawyer' في roles
```

كلها `security definer` مع `search_path` مثبّت.

مبادئ مكتوبة في الشيفرة:
* **من يقرّر ليس طرفاً**، وما تقرّر لا يُعدَّل — حتى من كتبه.
* **الزائر غير المسجَّل لا يكتب شيئاً**: هجرة 009 تسحب الصلاحية من دور `anon` بدل تركه
  يفشل على سياسة، حتى تكون رسالة الخطأ مفهومة (`permission denied` بدل `violates RLS`).
* **القاعدة تُكتب في قاعدة البيانات لا في الزر**: أي قاعدة مرسومة في الواجهة فقط
  يستطيع أي أحد استدعاء الـAPI حولها.

### صلاحية على مستوى العمود
`email` و`phone` **ليسا** ضمن الأعمدة الممنوحة لـ`anon`/`authenticated` على `profiles`.
نتيجة ذلك عملية: **`select=*` على `profiles` مرفوض** — الموقع يطلب الأعمدة بالاسم.
بيانات التواصل تُقرأ فقط عبر:
* `contact_book()` — قائمة الإدارة، والتحقّق **داخل** الدالة لا خارجها.
* `contact_of(target)` — للإدارة، أو للشخص نفسه، أو **للطرف الآخر في طلب بينهما**.
  هذا هو وعد المزاد مكتوباً حيث يُنفَّذ: «بياناتك تُسلَّم فقط للمحامي الذي قبلت عرضه».

### دوال RPC
| الدالة | ماذا تفعل |
|---|---|
| `my_profile()` | ملفي أنا |
| `contact_book()`, `contact_of()` | بيانات التواصل بين الأطراف — لا تُقرأ من الجدول مباشرة |
| `raise_notice()` | رفع إشعار لشخص آخر (لا يستطيع الكاتب قراءته) |
| `refund_under_guarantee(p_request)` | استرداد تحت الضمان؛ يُعيد كلمة: `refunded` / `not yours` / `window closed` / `not delivered` / `not offered` / `already disputed` |
| `has_ai(lawyer)` | هل اشتراك أداة الصياغة فعّال |
| `mentored_by(mentor, intern)` / `in_mentorship(id)` | هل بينهما إشراف فعّال / هل أنا طرف فيه |
| `mentored_hours(intern)` | ساعات الجلسات التي حضرها |
| `sponsorship_current(id)` | هل كفالة هذا الشهر مدفوعة |
| `charge_sponsorship(...)` | يكتب الدفعة والتوزيع؛ يُعيد `paid` / `not yours` / `not active` / `already paid` |
| `validate_promo_code(code, gross, type)` | قيمة الكود وسببه |
| `redeem_promo_code(code, request)` | صرفه؛ يُعيد `applied` / `already used` / `too late` / … |

---

## 5. الحالات والأدوار — القوائم الكاملة

**حالات الطلب `requests.status`:**
`new` → `quoting` → `assigned` → `scheduled` → `drafted` → `open_to_interns` →
`with_intern` → `in_progress` → `delivered` → `completed` \| `cancelled` \| `refunded`

**المجموعات الأربع** (`Models.requestPiles`) — كل طلب في واحدة فقط:

| المجموعة | ما يدخلها |
|---|---|
| `disputed` | عليه اعتراض مفتوح — تسبق كل شيء |
| `past` | `completed` \| `cancelled` \| `refunded` |
| `booked` | `scheduled`، أو `new` على خدمة مباشرة (مكالمة محجوزة لم تحن) |
| `live` | كل ما تبقّى |

**حالات الحساب `profiles.status`:** `pending` → `verified` \| `rejected`.

**الأدوار:** `client`, `lawyer`, `intern`, `staff`.
`staff` تُمنح ولا تُؤخذ ولا تُمنح ذاتياً. حساب واحد قد يحمل أكثر من دور، ويبدّل
«العرض بصفة» من صفحة الحساب — **تبديل عرض لا يمنح صلاحية**.

---

## 6. الصفحات — ماذا تفعل كل واحدة

| الصفحة | لمن | ماذا تفعل |
|---|---|---|
| `index.html` | الجميع | عنوان واحد وثلاث هبوطات: الزائر/العميل يرى صفحة قصيرة (ما هي المنصة، ثلاثة مداخل، محامون، مقالات)؛ المحامي والمتدرب يريان **لوحة عمل** بدلها. |
| `lawyers.html` | الجميع | الدليل + لوحة الترتيب. **الترتيب لا يُخزَّن**: يُحسب في كل رسم من التقييم (٦٠) والعمل المُسلَّم (٢٥) وسرعة الرد (١٥). |
| `lawyer.html` | الجميع | صفحة محامٍ: الرخصة، التخصصات، الخدمات وأسعارها، التقييمات، المقالات. |
| `intern.html` | الجميع | صفحة متدرب: المهارات، الساعات، الشهادات. تبويب «الأجر» يظهر للطرفين فقط. |
| `services.html` | الجميع | سوق الخدمات؛ للمحامي: قائمته هو مع ضبط السعر داخل النطاق. |
| `quotes.html` | العميل/المحامي | **المزاد العكسي**: العميل ينشر ملخصاً، المحامون المؤهلون يرون القضية ولا يرون الشخص، عروض تصل والساعة تعمل، ثم يقبل العميل عرضاً فيتحوّل إلى طلب. |
| `requests.html` | ثلاث واجهات في عنوان واحد | **العميل**: مجموعاته الأربع، المحادثة، «أين وصل طلبك»، القبول/التعديل/الاعتراض/التقييم. **المحامي**: صندوق الوارد + لوحته. **المتدرب**: مهامه + المهام المفتوحة للمنافسة. |
| `call.html` | طرفا الطلب | غرفة المكالمة (§9). |
| `assistant.html` | المحامي فقط | أداة الصياغة. العميل لا يصل إليها ولا يرى الكلمة. **حالياً محاكاة**: تأخير ثم `status='drafted'` بنص قالب، بلا نداء لأي نموذج. |
| `editor.html` | محامٍ/متدرب | تحرير المقالات. |
| `blog.html`, `article.html` | الجميع | المدونة، والتعليقات بأسماء حقيقية. |
| `admin.html` | `staff` فقط | مكتب الإدارة (§12). |
| `account.html` | مسجَّل | من أنت، تبديل «العرض بصفة»، الخروج. |
| `login.html`, `signup.html` | زائر | الدخول (بريد/كلمة مرور أو Google) والتسجيل بمعالج ثلاث خطوات تتفرّع حسب الدور. |
| `about.html` | الجميع | عن المنصة والأسئلة الشائعة. |
| `verify.html` | **محلياً فقط** | فحص ذاتي للاتصال بـSupabase. **يُحذف عند النشر** لأنه ينشئ حسابات ويكتب صفوفاً حقيقية. |

---

## 7. رحلة الطلب كاملة

### أ) الطريق المباشر
1. العميل يفتح صفحة محامٍ ويطلب خدمة → `requests` بحالة `new`، حدث `placed`، ورقم مرجعي.
2. أول تغيير يجريه المحامي على الطلب = **استلامه**: حدث `taken` مرة واحدة إلى الأبد (هجرة 016).
3. اختيارياً: يوجّهه لمتدرب (`assigned_to` + `intern_share`) → `with_intern`، أو يفتحه
   لكل المتدربين (`open_to_interns`) فيتنافسون عبر `applications`.
4. العمل ثم `delivered` + `body` → تُختم `delivered_at`.
5. العميل: يقبل (`completed`) أو يطلب تعديلاً **مرة واحدة** أو يعترض.
   **الصمت قبول**: ثلاثة أيام عمل (الجمعة والسبت لا تُحسبان).
6. التقييم يُغلق الملف ويسمح بطلب جديد.

### ب) المزاد العكسي
1. `quotes` بحالة `open` وساعة انتهاء.
2. المحامون المؤهلون (نفس التصنيف ونفس القناة) يقدّمون `offers`، وبعضها تلقائي.
3. العميل يقبل عرضاً → `quotes.status='accepted'` + `request_id` + إنشاء الطلب.
4. سطر المزاد يظهر في مقدّمة السجل: `brief_posted` → `offer_made`/`offer_auto` → `offer_taken`.

---

## 8. المحادثات

* محادثتان لكل طلب (`parties` و`internal`) + جمهور ثالث للإدارة (`staff`).
* كلاهما يكتب نصاً، يرفع ملفات (٢٥ ميجابايت للملف)، **ويسجّل مقطعاً صوتياً** من الميكروفون.
* الملفات المختارة **تنتظر الرسالة**: سطر وثلاثة مرفقات تصل كشيء واحد لا كأربعة.
* العرض: رسائلك على جهة والطرف الآخر على الجهة الأخرى بصورته، الاسم مرة واحدة فوق
  مجموعة رسائله، الساعة على السطر، واليوم فاصل فوق مجموعته.
* المرفقات: صوت يُشغَّل في مكانه، فيديو يُشاهَد في مكانه، والبقية تُفتح برابط موقّع.
* المتدرب المكلّف يُسلَّم القضية كاملة: ما طلبه العميل، محادثة العميل والمحامي، المرفقات —
  **للاطلاع**، والرد على العميل من المحامي.

---

## 9. المكالمات

* **الاتصال**: WebRTC مباشر بين المتصفحين (`assets/js/core/rtc.js`).
* **الإشارات**: `assets/js/core/signal.js` بواجهة واحدة `Signal.open(room, onMessage)`
  وناقلين: `BroadcastChannel` بين تبويبين على نفس الجهاز (للتطوير والاختبار)، و**Supabase
  Realtime** عبر الإنترنت. الاختيار من الإعدادات لا من نجاح تحميل مكتبة.
* **تصادم العروض**: الطرف «المهذّب» يتراجع — يُقارَن المعرّفان.
* **TURN**: مكان مخصص في `config.js` للمرحّل حين لا ينجح الاتصال المباشر.
* **التسجيل**: طرف واحد فقط يسجّل (صاحب المعرّف الأصغر)، ويخلط الصوتين عبر
  `AudioContext` ويضيف صورة الطرف الآخر في مكالمة الفيديو.
* **الإفصاح إجباري**: شريط ظاهر للطرفين طوال المكالمة؛ المسجِّل يبلّغ الطرف الآخر عبر
  قناة الإشارات نفسها (`{note:"taping"}`).
* **بعد المكالمة**: يُسأل الطرفان «نحفظها في الطلب؟» → نسخة `parties`.
  و**نسخة `staff` تُكتب دائماً**، ويُقال ذلك في نفس الجملة لا تحت السطر.
* التسجيل يدخل المحادثة **كرسالة يتدلّى منها الملف**، لا كمرفق سائب.

---

## 10. الاعتراضات والمال

### المعادلة (كل المبالغ بالهللات)

```
gross        = السعر × 100
serviceVat   = ضريبة على العمل — فقط إن كان المحامي مسجّلاً ضريبياً
client       = gross + serviceVat            ← ما يدفعه العميل
commission   = gross × commission_pct        ← عمولة المنصة (≤ ١٠٪)
commissionVat= commission × vat_pct
intern       = gross × intern_share          ← إن كان هناك متدرب بنسبة
lawyer       = gross − commission − commissionVat − intern
```

**عمل تحت اتفاق دائم بين المحامي والمتدرب لا يخصم من الطلب.**

### التسوية بعد قرار (`Models.settlement`)

```
lawyerPct = release ? 100 : refund ? 0 : القرار
kept      = gross × lawyerPct
refund    = client − kept
commission= kept × commission_pct
lawyer    = kept − commission − commissionVat − intern
```

**نصيب المتدرب يتحمّله المحامي مهما كان القرار — لا يُخصم من المتدرب.**

### الدفاتر
`Models.books(months)` تجمع الطلبات المحتسَبة **داخل المدة المختارة**، وتقرأ
`settlement` لا `distribute` — لأن طلباً مُستردّاً بالكامل كان يُحسب إيراداً.
`Models.series(months)` تقسّمها إلى أعمدة زمنية (أسابيع لشهر، أشهر لما فوقه).

### الضمان
`guarantee_hours` (٢٤ افتراضاً) من لحظة التسليم، `guarantee_unconditional`.
العميل يضغط زراً → `refund_under_guarantee()` → استرداد كامل + اعتراض مقرَّر في السجل.

---

## 11. قاعدة «طلب واحد في المرة»

هجرة 017: `guard_one_open_request()` و`guard_one_open_brief()`.
العميل لا يفتح طلباً جديداً ولا ينشر مزاداً حتى:
1. يُغلق طلبه السابق، **و**
2. يقيّم المحامي (`mark_request_rated()` يكتب `requests.rated`).

الإدارة والمسارات الموثوقة (`auth.uid() is null`) مستثناة.

---

## 12. مكتب الإدارة (`admin.html`)

ستة أقسام:

1. **نظرة عامة** — ستة مؤشرات + **رسومات حقيقية**: أعمدة الفترة (ارتفاع العمود ما دفعه
   العملاء، والجزء الغامق عمولة المنصة، والرقم فوقه عدد الطلبات)، حلقة «إلى أين ذهب المال»،
   شريط الطلبات مقسّماً، وترتيب الأكثر تسليماً. الرسومات **مرسومة يدوياً** (divs + SVG)،
   بلا مكتبة، بمتغيرات CSS تتبع الثيم. ثم «يحتاج قرارك» و«سجلّ ما جرى».
2. **الأشخاص** — كل من سجّل، الاعتماد والرفض، التمييز، تفعيل أداة الصياغة.
   **الضغط على أي اسم يفتح ملفه**: كل طلب له فيه يد، دوره فيه، اعتراضاته، وكل ما فعله.
3. **الطلبات** — أربعة تبويبات منفصلة: جارية / محجوزة / سابقة / اعتراضات.
   تفتح على الاعتراضات إن وُجد اعتراض ينتظر قراراً.
   في ملف القضية: **مقعد القراءة** — العميل، المحامي، المتدرب، أو نسخة المنصة —
   وتُرسم المحادثة **نفسها** كما يراها صاحبها (نفس المكوّن، معرّف مختلف)، لا نسخة عنها.
   القرار: `release` / `refund` / `split` بنسبة، **مع سبب مكتوب إجباري**.
4. **المالية** — الداخل، الخارج، رسوم البوابة، التكاليف، الصافي، حصص الشركاء.
5. **الإعلانات** و**الخدمات** (الكتالوج والنطاقات السعرية).
6. **الإعدادات** — العمولة، الضريبة، رسوم البوابة، مدة الضمان، سنوات الخبرة الدنيا.

---

## 13. لوحة المحامي والمتدرب

شريط فوق قائمة العمل يقول:
* **مستحق لك** (عمل قُبِل) و**تحت التنفيذ** (وعد لا رصيد) — كلاهما **نصيبه بعد الخصم**،
  ومكتوب تحته صراحة أنه ليس ما يدفعه العميل.
* المجموعات الأربع بعددها.
* التقييم والمرتبة.
* للمتدرب: الساعات المسجّلة ونسبة التقدّم نحو الشهادة (٤٠ ساعة).

---

## 14. اللغة والثيم والوصول

* قاموسان كاملان (`ar`, `en`) داخل `i18n.js`. الإضافة عبر `tools/i18n_add.py` الذي يرفض
  مفتاحاً مكرراً حتى لا ينحرف القاموسان.
* الترجمة بالسمات: `data-i18n`, `data-i18n-html`, `data-i18n-attr`, `data-i18n-vars`.
* الأرقام دائماً لاتينية (`ar-SA-u-nu-latn`)، والتواريخ ميلادية بالشهر مكتوباً.
* الاتجاه ينقلب مع اللغة (`dir="rtl"`/`ltr`)، وكل CSS يستعمل الخصائص المنطقية
  (`inline-start`, `border-end-start-radius`) لا `left`/`right`.
* الثيم: `data-theme="light|dark"` + متغيرات CSS، ويُقرأ قبل أول رسم.

---

## 15. الأمان — قواعد غير قابلة للتفاوض

1. **المفتاح السري (`sb_secret_…`) لا يوضع في `config.js` ولا في المستودع ولا في محادثة.**
   يتجاوز كل السياسات. الموجود في الصفحة هو `sb_publishable_…` وهذا صحيح ومقصود.
2. **كلمات المرور لا تمرّ بشيفرة المشروع** — تذهب إلى Supabase Auth مباشرة.
3. **`verify.html` لا يُنشر.** الـworkflow يحذفه من `_site`.
   ⚠️ **إن نُشر الموقع على Vercel أو أي مستضيف آخر بدل GitHub Actions، فلن يُحذف** —
   احذفه يدوياً أو امنع الوصول إليه.
4. كل قاعدة تهمّ مكتوبة كسياسة صف، لا كزر مخفي.
5. الإشعارات لا تحمل محتوى.

---

## 16. النشر

`.github/workflows/pages.yml` عند كل دفعة إلى `claude/website-dark-light-languages-5exre8`:
1. يبني الحزمة أحادية الملف (`tools/build-artifact.mjs` → `dist/sanad.html`).
2. ينسخ الصفحات والأصول إلى `_site`، **ويحذف `verify.html`**.
3. يبصم كل رابط أصل برقم الـcommit (`?v=abc1234`) حتى لا يعمل متصفح بـHTML جديد
   وJavaScript قديم.
4. يدفع `_site` إلى فرع `gh-pages`.

الموقع: `https://engfm0.github.io/law/`

**للنشر على Vercel**: المشروع ثابت بالكامل — Framework: Other، Root: `./`، بلا أمر بناء
وبلا متغيرات بيئة (المفاتيح داخل `config.js` أصلاً). التنبيه الوحيد هو `verify.html` أعلاه.

---

## 17. الاختبارات

| النوع | العدد | الأمر |
|---|---|---|
| منطق بلا متصفح | ٧ حزم | `node tools/model-test.mjs` … |
| متصفح (Playwright) | ٢٩ حزمة، ~٦٠٠ تحقّق | `sh tools/run-all.sh` |
| صلاحيات قاعدة البيانات | ١٦ ملف، ٢٧١ تحقّق | `psql -f supabase/rls-test*.sql` |
| الهجرات على بيانات حقيقية | ١٩/١٩ | `sh tools/migrate-test.sh` |

حزم يُنظر إليها أولاً عند التعديل: `thread-e2e` (المحادثات)، `call-e2e` (المكالمات
والتسجيل)، `desk-e2e` (لوحة الإدارة والمقاعد)، `record-e2e` (السجل والرقم المرجعي)،
`pay-e2e` (الحصص)، `sweep-e2e` (٢٤٠ تركيبة صفحة×دور×لغة×ثيم).

---

## 18. ما ليس موجوداً بعد (لا تفترض وجوده)

* **بوابة دفع حقيقية** — الجدولان جاهزان ولهما الآن كاتب واحد حقيقي
  (`charge_sponsorship`)، لكن لا اتصال ببوّابة فعلية بعد: الشحن يقيّد الدفعة ولا يحصّلها.
* **ذكاء اصطناعي حقيقي في `assistant.html`** — محاكاة بتأخير ونص قالب.
* **خادم TURN مشغَّل** — المكان محجوز في `config.js`.
* **بريد أو رسائل نصية** — الإشعارات داخل الموقع فقط.
* **تطبيق جوال** — الموقع متجاوب فقط.

---

## 19. مسرد سريع للنموذج الذي سيكتب التعديلات

| تريد أن تعدّل… | ابدأ من |
|---|---|
| منطق مشتق (سعر، حالة، ترتيب، حساب) | `assets/js/data/models.js` |
| مكوّن مرئي مشترك (محادثة، سجل، فقاعة، رسم بياني) | `assets/js/ui/components.js` |
| نصاً ظاهراً | `assets/js/core/i18n.js` عبر `tools/i18n_add.py` |
| شكلاً | `assets/css/style.css` (متغيرات في `:root` و`[data-theme="dark"]`) |
| قاعدة تخصّ من يقرأ ماذا | هجرة جديدة في `supabase/migrations/` **+ ملف `rls-test-NNN.sql`** |
| صفحة | `assets/js/pages/<name>.js` — والملف `.html` يبقى هيكلاً فارغاً |
| شكل صف قادم من قاعدة البيانات | `assets/js/core/store.remote.js` (`inX`/`outX`) |

**أعراف الشيفرة:** ES5 (`var`, `function`)، لا `class`، لا سهم، لا قوالب نصية —
سلاسل بـ`+`. كل نص ظاهر يمرّ بـ`I18N.t`. كل إدخال مستخدم يمرّ بـ`App.esc`.
كل مبلغ بالهللات. أي هجرة **يجب أن تكون قابلة لإعادة التشغيل** (`if not exists`,
`create or replace`, `drop policy if exists`).
